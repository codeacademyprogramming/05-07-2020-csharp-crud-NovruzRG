@model Crud.Models.Product

@{
    ViewBag.Title = "ToDelete";
}

<h2>ToDelete</h2>

<h3>Are you sure you want to delete this?</h3>
<div>
    <div>
        <div style="margin:10px">
            <table class="table">
                <thead>
                    <tr class="row">
                        <th class=" col-md-3"> @Html.DisplayNameFor(model => model.Name)</th>
                        <th class=" col-md-5">@Html.DisplayNameFor(model => model.Description)</th>
                        <th class=" col-md-2"> @Html.DisplayNameFor(model => model.Price)</th>
                        <th class=" col-md-3">Actions</th>
                    </tr>
                </thead>
                <tbody>
                    @{
                        <tr class="row">
                            <td class="col-md-3"> @Html.DisplayFor(model => model.Name)</td>
                            <td class="col-md-5"> @Html.DisplayFor(model => model.Description)n</td>
                            <td class="col-md-2"> @Html.DisplayFor(model => model.Price) </td>
                            <td class="col-md-3">
                                <asp:Button ID="Button1" runat="server" Text="Button" OnClientClick="return confirm('Are you sure?')" />
                                @Html.ActionLink("Delete", "Delete", new { id = Model.ID }, new { onclick = "return confirm('Are sure wants to delete?');" }) |
                                @Html.ActionLink("Back to List", "Index")
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>
